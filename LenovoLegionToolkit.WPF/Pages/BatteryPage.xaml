<Page
    x:Class="LenovoLegionToolkit.WPF.Pages.BatteryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpfui="clr-namespace:WPFUI.Controls;assembly=WPFUI"
    Title="Battery"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Style="{StaticResource UiPageScrollable}"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,16,12">
        <TextBlock
            Margin="0,16,0,24"
            FontSize="24"
            FontWeight="Medium"
            Text="Battery" />

        <StackPanel Margin="0,0,0,24" Orientation="Horizontal">
            <wpfui:SymbolIcon x:Name="_batteryIcon" FontSize="96" />
            <StackPanel
                Margin="16,0,0,0"
                VerticalAlignment="Center"
                Orientation="Vertical">
                <TextBlock
                    x:Name="_precentRemaining"
                    Margin="0,0,0,4"
                    FontSize="32"
                    FontWeight="Medium" />
                <TextBlock
                    x:Name="_status"
                    FontSize="16"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </StackPanel>

        <wpfui:CardControl
            Title="Battery temperature"
            Margin="0,0,0,24"
            Subtitle="Current temperature of the battery.">
            <TextBlock x:Name="_batteryTemperatureText" FontSize="14" />
        </wpfui:CardControl>

        <wpfui:CardControl
            Title="Discharge rate"
            Margin="0,0,0,8"
            Subtitle="Rate at which battery is charged (+), or discharged (-).">
            <TextBlock x:Name="_batteryDischargeRateText" FontSize="14" />
        </wpfui:CardControl>

        <wpfui:CardControl
            Title="Current capacity"
            Margin="0,0,0,8"
            Subtitle="Current battery capacity.">
            <TextBlock x:Name="_batteryCapacityText" FontSize="14" />
        </wpfui:CardControl>

        <wpfui:CardControl
            Title="Full charge capacity"
            Margin="0,0,0,8"
            Subtitle="Battery capacity when fully charged.">
            <TextBlock x:Name="_batteryFullChargeCapacityText" FontSize="14" />
        </wpfui:CardControl>

        <wpfui:CardControl
            Title="Design capacity"
            Margin="0,0,0,24"
            Subtitle="Battery capacity as designed.">
            <TextBlock x:Name="_batteryDesignCapacityText" FontSize="14" />
        </wpfui:CardControl>

        <wpfui:CardControl
            Title="Cycle count"
            Margin="0,0,0,8"
            Subtitle="Number of charge/discharge cycles.">
            <TextBlock x:Name="_batteryCycleCountText" FontSize="14" />
        </wpfui:CardControl>

    </StackPanel>
</Page>
